export default {
	modules: {
		itemBlacklist: {
			enabled: true,
			list: [],
            fixable: [],
            // The interval in ticks to check for items
            frequency: 0,
            // The events that this module will run on
            events: [],
        },

	},
};
